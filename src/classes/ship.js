class Ship {
    constructor(shipLength, token, name, orientation) {
        this.ShipLength = shipLength;
        this.token = token;
        this.name = name;
        this.orientation = orientation;
    }
}

export default Ship;